services:
    korva-vitrine:
        build:
            context: .
            dockerfile: Dockerfile
            target: production
        ports:
            - "8085:3000"
        restart: unless-stopped
